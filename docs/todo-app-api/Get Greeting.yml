info:
  name: Get Greeting
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/greeting"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
