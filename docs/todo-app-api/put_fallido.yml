info:
  name: put_fallido
  type: http
  seq: 9

http:
  method: PUT
  url: "{{baseUrl}}/api/items/this-id-does-not-exist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
