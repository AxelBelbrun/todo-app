info:
  name: Get All Items
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
